'use strict';

/**
 * last-recipient controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::last-recipient.last-recipient');
